#include<stdio.h>
    int main(){

        // char ch = '#';       // %d for integer, %f for float, %c for character
        // printf("%c", ch);

        //Ascii value
            char ch = '% ';
            printf("%d", ch);

        return 0;
    }